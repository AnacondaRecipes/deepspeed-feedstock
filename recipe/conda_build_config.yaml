# cuda_compiler_version values, paired with pytorch builds available on pkgs/main:
#   None  → CPU build (matches `pytorch * cpu_*`)
#   12.9  → CUDA 12.9 (matches `pytorch * gpu_cuda129_*`); linux-64 only
#   13.0  → CUDA 13.0 (matches `pytorch * gpu_cuda130_*`); linux-64 + linux-aarch64
cuda_compiler_version:
  - None
  - 12.9                       # [linux and x86_64]
  - 13.0                       # [linux]
